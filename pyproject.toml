[project]
name = "cardplay"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "alive",
    "asgiref>=3.11.1",
    "django>=6.0.2",
    "uvicorn>=0.40.0",
]

[tool.uv.sources]
alive = { path = "../alive", editable = true }
